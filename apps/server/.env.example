DATABASE_URL="postgresql://postgres:postgres@localhost:5432/function_planner"
# Optional. Needed when the DB role cannot CREATE DATABASE (cloud hosts) or for
# `prisma migrate diff --from-migrations`. Locally, CREATEDB on the role is enough.
# SHADOW_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/function_planner_shadow"
SESSION_SECRET="replace-me"
API_JWT_SECRET="replace-me"
CLIENT_URL="http://localhost:5174"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:3000/auth/google/callback"
